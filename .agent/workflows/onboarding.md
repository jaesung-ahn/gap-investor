---
description: Initial onboarding and environment verification
---

1. Check Java version
   // turbo
   `java -version`

2. Check Gradle wrapper
   // turbo
   `./gradlew --version`

3. Verify project builds
   `./gradlew build -x test`
